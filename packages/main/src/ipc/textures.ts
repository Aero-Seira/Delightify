import { ipcMain } from 'electron';

export function registerTexturesHandlers(): void {
  // Texture-related handlers will be added in Phase 2
  // textures are served via items:get-texture in the items handler
  console.log('Texture handlers registered (stub)');
}
